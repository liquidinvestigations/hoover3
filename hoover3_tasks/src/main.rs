// use hoover3_tasks::scan::start_worker;
// use hoover3_tasks::client::get_client;use tracing::info;use hoover3_database::tracing::init_tracing;
#[tokio::main]
async fn main() -> anyhow::Result<()> {
    // init_tracing();
    // let _client = get_client().await?;
    // info!("client created");
    // loop {
    // info!("main loop");
    // start_worker().await.unwrap();
    // }
    unreachable!()
}
